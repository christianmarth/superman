query SelectMerchant($id: Int!) {
  merchants_by_pk(id: $id) {
    assets {
      url
    }
    location
    name
    description
    email
    phone_number
    products {
      id
      name
    }
  }
}